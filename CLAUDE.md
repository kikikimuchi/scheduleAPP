# scheduleAPP - 作業ルール

## 食材・栄養データの登録について（最重要）

- **憶測で数値を決めない。** カロリー・糖質・脂質・タンパク質・内容量（缶/瓶/mlなど）を勝手に推測して確定値のように登録しない。
- 値がわからないときは**登録前に必ず調べる**（商品パッケージの栄養成分表示、メーカー公式、店舗公式サイトなど一次情報を優先）。
- 調べても確定値が存在しないもの（生の果物・量り売りなど）だけ、**「推定値」と明示**したうえで、根拠（換算元の100gあたりの値や想定グラム数）を添えて登録する。
- 提供形態（缶・瓶・コップ・サイズ）も推測しない。実際の提供形態を確認してから数値を出す。
- ユーザーに指摘されてから調べるのではなく、**出す前に確認する**。

理由: 栄養の数字はユーザーのダイエット記録そのもの。不正確な値は記録の信頼性を損なう。

## 食事記録の運用ルール（最重要・2026-09-21 追加）

- **記録するのは「食べた・飲んだ」とユーザーが明言したものだけ。** 質問だけされたもの・写真が送られただけのもの・会話に出ただけのものは絶対に記録しない。推測で穴埋めしない。
- **複数品をまとめて報告されたときは、記録する前に品目リストをユーザーに提示して確認を取る。** 表裏の写真で同じ商品が複数枚ある場合や、前日と同じ商品がある場合は特に慎重に数える。
- **記録の修正は、必ず元の報告（写真・本文）に戻って数え直してから行う。** 指摘されたからといって推測で消したり戻したりしない。修正前に「今こうなっている→こう直す」を示して確認を取る。
- **修正後は必ずサーバーから再取得して、全品名と合計を提示して確認する。**
- **数字が変わったときは「そのまま」で流さない。** ユーザーはその数字で次の食事を決めている。変更後の合計と、次の食事にどう影響するかを必ず計算して伝える。
- ミスをしたときは軽い調子（「すまん」「😂」など）で流さない。何を間違えたか、どう直したか、再発防止を具体的に述べる。

理由: 2026-09-21、写真の照合ミスで同じ記録を3回書き換え、ユーザーの食事計画と信頼を損なった。記録の正確さがこのダイエットの土台であり、二度と繰り返さない。

## 時間・時刻の扱い（重要）

- **時間や時間帯に関わる発言をする前に、必ず実際の時刻を確認する。** 「おやすみ」「今日」「夜」「朝」「もう遅い」など、時刻・日付に依存する言葉を使う前に `TZ=Asia/Tokyo date` で現在時刻（JST）を確認してから言う。
- ユーザーのタイムゾーンは **日本（JST / Asia/Tokyo）**。コンテナはUTCなので、日付・時刻は必ずJSTに変換して考える。
- 食事・体重・起床などを「今日」に記録するときも、憶測せずJSTの現在日付を確認してから日付を決める。

理由: 時刻を確認せずに「おやすみ」等と言うと実態とズレて信頼を損なう（例: 実際は朝6時なのに夜前提で話す）。

## アプリ構成メモ

- Firestore プロジェクト: `keiriauto-6f8f1`（OPEN ルール、REST で直接書き込み可）
- 食材マスターのコレクション名は **`schedule_foodMenus`**（`foodMenus` ではない）。ドキュメントIDは食材の `id`（数値）、フィールド: `id, category(main|snack|drink), name, yomi, kcal, carbs(糖質), fat, protein`。
- 体重は `schedule_weights`、ドキュメントID = 日付（例 `2026-06-25`）、フィールド: `date, weight, bodyFat` 等。
- 検索は name＋yomi をひらがな正規化して部分一致。カタカナ商品はひらがな読みを `yomi` に入れる。
- GitHub Pages は `main` ブランチからデプロイ。変更はフィーチャーブランチと `main` の両方に push する。
